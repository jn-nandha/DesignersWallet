<h1>Notification</h1> Login as: <%= current_user.email%>
<% if @users.count != 0 %>
	<div style="font-size: 25px;line-height: 40px;">
		<% @users.each do |u| %>
		 <%= link_to u.name, chat_path(recp: u.id) %><br />
		<% end %>
	</div>
<% else %>
	<h2>No new Notification</h2>
<% end %>