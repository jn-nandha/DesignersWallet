<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>Designer's wallet</title>

  <!-- Main css styles -->
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>

  <!-- Main javascript files -->
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

</head>
<body>
	<!-- Wrapper-->
    <div class="wrapper wrapper-content gray-bg" style="margin-top: 60px;">

        <!-- Page wraper -->
        <div class="container">

            <!-- Page wrapper -->
            <%= render 'layouts/topnavigation' %>
            <% if notice %>
            <%= notice %>
            <% end %>
            <% if alert %>
            <%= alert %>
            <% end %>

            <!-- Main view  -->
            <%= yield %>
        </div>
        <!-- End page wrapper-->

    </div>
    <!-- End wrapper-->
</body>
</html>
